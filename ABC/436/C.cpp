#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i,n) for(int i=0; i<(n); i++)

void solve() {
    ll N, M;
    cin >> N >> M;

    set<pair<ll, ll>> block;
    ll ans = 0;

    for(ll i=0; i<M; i++) {
        ll r, c;
        cin >> r >> c;

        
    }
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();

    return 0;
}